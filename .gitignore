# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual envs
.env
.venv
env/
venv/
ENV/

# Unit test / coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Type checkers / linters
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# Environments secrets
.env.local
.env.*.local
*.pem

# Databases / local state
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
*.db-shm

# Benchmark working files (datasets + retrieval logs are inputs/outputs, not source)
runners/**/_cache/
*.retrievallog.jsonl
*_retrievallog_*.jsonl

# Node (commitlint may be installed locally)
node_modules/
npm-debug.log*

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Private planning surface — must NEVER be committed to this public repo.
.agent-shared/

# === Sovantica portfolio — defensive .gitignore patterns ===
# Generated by 0.1.0 (2026-06-29T18:21:01Z) from a shared template.
# Appended idempotently by sync-agent-files.sh — do not duplicate by hand.

# Private agent infrastructure — must never enter a public repo working tree.
.agent-overrides.yaml
.agent-overrides.yml

# Template artefacts — should only ever exist in private workspace.
*.tmpl
*.template.md

# Legacy private agent files (pre-generator era).
.agent/
*.private.md
*-internal.md

# Generator local cache.
.agent-sync.lock
.agent-sync.cache

# Local environment overrides.
