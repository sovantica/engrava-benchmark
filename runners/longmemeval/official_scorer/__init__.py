"""Official LongMemEval scoring contract (upstream-verbatim, pinned).

See ``UPSTREAM.md`` for the upstream repo + the pinned commit recorded as
``scorer_version``. This package reproduces the official judge prompts verbatim;
it does not re-author or modify the scoring rubric.
"""
